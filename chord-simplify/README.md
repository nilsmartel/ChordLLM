# Chord Simplify

Tool to simplify chords.


Takes a list of chords as input (stdin) and outputs simplified chords on stdout.
First argument is a file containing chords that should not be simplified.

## Rules

- Remove digits
    - Em7 => Em
